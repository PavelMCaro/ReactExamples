import styled from 'styled-components';

export const Wrapper = styled.div`
    width: 60%;
    display: flex;
    padding: 10px 15px;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
`;